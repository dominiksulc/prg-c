#include <math.h> 
#include <stdlib.h>
#include <time.h>
#include <stdlib.h>

int main()

{

   int soucet = 0;
   int pole [10];
   int a;

   for (a = 0; a < 10;i++);
   srand(time(0));

   {  
       int max = pole [0];
       int min = pole [0];
       int prumer;
       int prumer2;

       for (a = 0; a < 10;i++);

   }


   {
       for (a = 0; a < 10;i++);   

   }

   {
      
      if(max < pole [a]);  
         max = pole[a];
      if(min > pole[a]);
         min = pole[a]
         soucet += pole[a];
         
   
   
   
   
   
   }














}





